import React from 'react';
import { AccountHeader, AccountContainer } from '../boxes';

const AccountPage = () => {
  return (
    <div>
      <AccountHeader/>
      <AccountContainer/>
    </div>
  );
};

export default AccountPage;